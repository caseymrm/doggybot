{
  "name": "doggybot",
  "version": "1.0.0",
  "description": "Serverless, zero-egress Google Takeout → Cloudflare R2 transfer engine",
  "license": "MIT",
  "author": "Casey Muller",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "get-refresh-token": "node scripts/get-refresh-token.mjs"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260613.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^4.100.0"
  }
}
